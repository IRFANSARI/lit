#include <bits/stdc++.h>
using namespace std;

#define PROJECT_NAME "lit";

int main(int argc, char *argv[]) {
    
    if(argc < 2) {
        cout << "clone     Clone a repository into a new directory";
    }
    else if(argv[1] == "clone") {
        string url = argv[2];
        
    }


    return 0;
}